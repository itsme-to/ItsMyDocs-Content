export const sections = []

## Commands

<Properties>
  <Property name="schem <schematic> <current-version> <to-version> [to-versions]" type="schematic">
    Converts a schematic from one version to another or multiple versions.
  </Property>
  <Property name="world <schematic> <current-version> <to-version> [to-versions]">
    Converts a schematic to a world from one version to another or multiple versions.
  </Property>
  <Property name="testblock <block> <from-version> <to-version">
    Tests a block conversion from one version to another.
  </Property>
  <Property name="reload">
    Reloads the configuration.
  </Property>
  <Property name="end" type="quit">
    Quits the program.
  </Property>
</Properties>
