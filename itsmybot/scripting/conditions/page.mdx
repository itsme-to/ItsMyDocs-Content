## All Conditions

export const sections = []

<Properties>
  <Property name="aboveMembers" type="Required Args: amount - number">
    Check if the amount of members in a guild is above a certain number.
  </Property>
  <Property name="bellowMembers" type="Required Args: amount - number">
    Check if the amount of members in a guild is bellow a certain number.
  </Property>
  <Property name="content" type="Required Args: text - string[]">
    Check if a content of an action equals a certain text.
  </Property>
  <Property name="contentContains" type="Required Args: text - string[]">
    Check if a content of an action contains a certain text.
  </Property>
  <Property name="expression" type="Required Args: input - string, operator - string, output - string">
    Check if an expression is true.
  </Property>
  <Property name="hasRole" type="Required Args: role - string, Optionnal Args: inherit - boolean">
    Check if a user has a certain role.
  </Property>
  <Property name="isBot">
    Check if a user is a bot.
  </Property>
  <Property name="inTicket">
    Check if a message is in a ticket. Requires [Tickets](https://builtbybit.com/resources/51625/) plugin.
  </Property>
</Properties>